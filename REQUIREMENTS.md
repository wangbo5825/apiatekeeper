# ApiGateKeeper 需求记录（进行中）

> 此文件随用户描述持续更新，最终并入正式规划。
> 2026-08-29：项目由 apigate 更名为 Charon；引入多应用（Application）模型。
> 2026-08-29：更名为 API Gatekeeper；引入应用缺省配置、自动加入规则、
> 2026-09-04：正式更名为 **ApiGateKeeper**。
> 变量（上下文）模型与多规则绑定。

## 定位

- 轻量级多应用 API 网关（正式名 **ApiGateKeeper**，原 apigate），不采用 API Platform 等重型框架
- 请求必须先命中某个应用（Application）才会被处理

## 已确认需求

### 1. 缺省透明代理

- 网关缺省行为等同于标准 proxy：任何 HTTP 请求透明通过网关，访问真实服务器
- 不做默认拦截、改写、缓存
- （V2 修订）全局缺省不再透传：请求必须先命中一个应用；未命中 → 404。
  应用内选择“前缀放行”时，等价于原透明代理；选择“清单放行”时，
  仅 API 列表中配置的请求放行

### 2. 请求记录

- 透明代理的同时记录请求数据
- 支撑后台生成 API 列表、客户端列表及其统计信息

### 3. 后台信息

- API 列表 + 统计
- 客户端列表 + 统计

### 4. 规则制定

- 管理员基于统计信息制定规则

#### 规则类型 1：API 缓存策略

- 多种策略：
  - 自动按协议（遵循 HTTP 缓存协议）
  - 不缓存
  - 强制缓存一定时间（固定 TTL）

#### 规则类型 2：API 限流策略

- 限流维度（可单因素，可多因素组合）：
  - 客户端 IP
  - URL 中的特定参数值（query 参数 / 路径参数）
  - 多因素结合（如 IP + 参数值）
- 限流参数：时间窗口、阈值、突发（burst）
- 超出阈值的行为（预期 429，待确认响应格式）

#### 规则类型 3：黑名单

- 支持 IP / URL 参数值 / 组合维度加入黑名单
- 命中黑名单直接拦截
- 是否支持“超限自动拉黑”（自动将超限对象加入黑名单）及拉黑时长，待确认

#### 规则类型 4：URL 变量保持 + 动态访问许可

- 根据 URL 中的指定规则提取并保持变量（命名捕获：路径参数 / query 参数）
- 变量具有生命期（TTL），过期自动失效
- 其他 URL 可基于这些变量动态设置访问许可，例如：
  - 请求 A 命中提取规则 → 生成/刷新变量（如 `code=xxx`，TTL 5 分钟）
  - 请求 B 命中许可规则 → 校验变量是否存在且未过期 → 放行 / 拒绝
- 许可判断的具体条件（存在性 / 值匹配 / 剩余生命期）待细化

#### 规则类型 5：URL 认证规则

- 按 URL 规则设置是否要求认证
- 支持多种认证方式：
  - Token（不透明令牌，校验网关内的有效令牌存储）
  - JWT（校验签名、issuer、audience、过期时间）
  - 其他（待补充）
- 认证失败预期返回 401
- 待细化：token 的传递位置（Authorization 头 / query 参数 / Cookie）、是否与规则类型 4 的变量联动

### 6. 多上游与健康检查（2026-08-29 确认新增）

- 网关支持配置多个上游目标（backend / target）
- 上游健康检查（主动探测 / 被动失败计数，具体策略待细化）
- 主备切换：主上游不可用时自动切换到备用上游
- 多目标轮询：同一上游组内按轮询（round-robin）分发
- 与现有规则的 `backend` 参数结合：规则可指定 backend，未指定时走默认上游组
- 上游组定义包含**缺省映射关系**（2026-08-29 确认）：
  - 主要是前缀转写：剥离应用 base_path（/v1 → 空）/ 前缀替换（/v1 → /api/v1）/
    加前缀
  - 未针对 API 单独转写时，转发使用该缺省映射
  - API 级转写（change_target 规则或 API 独立配置）优先于缺省映射

### 7. Prometheus 指标（2026-08-29 确认新增）

- 暴露 Prometheus 格式指标端点（如 `/admin/api/metrics`），供统一监控 / 告警
- 建议指标：QPS、延迟分位（P50/P95/P99）、4xx/5xx 计数、429/403 命中、
  限流超限次数、上游健康状态

### 8. 多应用（Application）模型（2026-08-29 确认）

- 请求必须命中某个应用才会被处理，未命中 → 404
- 应用标识：base_path（起始路径，如 `/v1`）+ 可选 host / 请求头匹配
- 应用内两种放行模式：
  - 前缀放行（默认）：base_path 下所有请求放行（即“大规则”），
    详细规则（限流 / 认证 / 缓存等）按需叠加
  - 清单放行：只有应用 API 列表中配置的 API 才放行
- 每种应用独立定义：
  - 上游服务器（多目标轮询 / 主备切换 + 健康检查）
  - 详细 API 列表（应用内相对路径，如 `GET /orders/{id}`）
  - 规则（含起始规则 / 详细规则）
  - 黑名单
  - 令牌
- 应用内规则使用相对 base_path 的路径模式
- 数据模型：新增 apps / upstreams 表；rules / apis / blacklist / tokens /
  request_logs / 统计表均增加 app_id
- 管理端：应用管理 + 应用详情（概览 / API / 客户端 / 规则 / 黑名单 / 令牌 / 上游）
- 待细化：host / 请求头匹配的具体规则、清单放行时的默认拒绝响应码

### 9. 应用统一缺省配置（2026-08-29 确认）

每个应用具备一套统一缺省配置：

- 缺省是否放行（default_allow，默认放行）
- 返回数据是否缓存及缓存时间（default_cache_enabled / default_cache_ttl）
- 缺省是否自动加入规则（auto_add_rules，默认开启）

缺省配置与自动加入的组合行为：

| default_allow | auto_add_rules | 效果 |
|---|---|---|
| 放行 | 开 | 新 URL 自动放行并加入 API 列表（零配置可用） |
| 放行 | 关 | 新 URL 照常放行，但不加入 API 列表 |
| 拒绝 | 开 | 新 URL 拒绝；加入列表后放行 |
| 拒绝 | 关 | 严格白名单，仅列表内 API 可用 |

### 10. 自动加入规则（零配置可用）

- 正常缺省（放行 + 自动加入）：请求自动访问应用指定的上游服务，
  并按照 URL 模式（归一化后）自动加入应用 API 列表
- 用户无需额外配置即可立即使用网关，并获得所有可用 API 列表
- 关闭自动加入规则后，新的 API 不再加入 API 列表（已登记 API 保留）

### 11. API 列表编辑与冲突检测

- 管理员可对应用 API 列表进行选择与编辑
  （方法 / 路径模板 / 名称 / 独立覆盖配置）
- 保存编辑后的 API 时，检测其是否与其他 URL 冲突：
  - 完全重复（同方法同模板）
  - 模板互相覆盖（如 `/orders/{id}` 与 `/orders/123`、通配重叠）
- 检测到冲突：拒绝保存并提示冲突项；允许删除冲突的 API
- 删除后：若 auto_add_rules 开启且放行，流量再次出现时会重新登记
  （待确认：是否提供“删除后阻止再登记”选项）

### 12. 规则与变量（上下文）模型（2026-08-29 确认）

- 每个 API 可应用多条规则：rate_limit / cache / auth / 统计计数 /
  保存变量 / 改变请求目标 / 响应加工 等
- 规则执行需要上下文，上下文由变量构成：
  - 请求级变量：请求 API URL、客户端 IP、userid、apikey、method、
    header / query 参数
  - 关联变量（按 key 存储）：如按客户端 IP 保存变量 `last_url`；
    按 userid 保存变量 `last_ip` —— 即针对某个特定 key 的变量
- 应用内提供“变量声明区域”：声明命名变量（key 维度、取值来源、TTL），
  供规则提取变量值
- 规则参数可按变量控制：如 rate_limit 可按 ip、userid、ip+userid 组合，
  或引用声明的变量（`var:<name>`）
- （2026-08-29 确认）变量模型：
  - 请求级变量（内置，无需存储）：`req.url` / `req.method` / `req.ip` /
    `req.userid` / `req.apikey` / `req.header:<name>` / `req.query:<name>` /
    `req.path:<name>`
  - 系统（持久化）变量：按 key 维度存储；key 维度 = 任意请求级变量
    （ip / url / query 参数值 / header 参数值 / userid / apikey）
  - 可见性规则：同一 key 值下的请求共享同一槽位 ——
    "同 IP 上下文 / 同 URL 上下文 / 同 query 变量上下文 / 同 header 变量上下文"
  - 跨请求关联靠 key 值一致，不靠显式关联
- （2026-08-29 确认）维度定义（key 的来源）分三类：
  - 标准维度（内置）：`req.ip` / `req.url` / `req.method`，请求中必然可解析
  - 参数化维度：`req.query:<name>` / `req.header:<name>` / `req.path:<name>`，
    参数名在声明时写死（维度必须是确定字符串，不能用“任意参数”）
  - 语义维度（应用内声明，解析链）：如 `dim:user = [req.userid →
    req.header:X-User-Id → req.query:uid]`，按顺序取第一个非空值，
    解决“userid 在不同接口字段名不同”的问题
- 变量声明的 key 维度引用上述三类维度之一；规则引用 `var:<name>` 时，
  经声明的维度解析出当前请求的 key 值再查槽位
- 规则类型与各规则的详细配置方法见 docs/RULES.md
- 待确认：变量声明区域作用域（应用级 / 全局）、变量命名与冲突检测、
  删除后阻止再登记

### 13. 响应加工（filter 阶段规则）（2026-08-29 确认）

- 规则分两个执行阶段：
  - access（请求控制）：限流 / 认证 / 黑名单 / 变量许可 / 缓存策略 /
    保存请求变量 / 改变请求目标
  - filter（响应加工）：对返回内容进行处理与转换
- filter 阶段规则类型 `response_transform`，支持操作（op）：
  - `replace`：替换返回 JSON 中某个属性值（静态值或变量值 `var:<name>`）
  - `save_variable`：把 JSON 中某个属性值保存到变量
    （与变量声明区域联动，按 key 维度存储）
  - `convert`：JSON ↔ XML 互转
- 转换后自动处理 Content-Type（如 JSON→XML 输出 application/xml）
- filter 加工仅在 buffer 模式可用（契约传响应 body）；stream 模式退化为仅记录
- （2026-08-29 确认）**从返回结果中设置变量为必须实现特性**：
  response_transform op=save_variable（把响应 JSON 属性保存到声明变量）
- 待细化：响应 body 读取上限、非法 JSON/XML 的处理（透传 vs 报错）、
  JSON 属性路径语法（点号 / 数组下标）、属性脱敏是否需要单独 op

## 待补充

- 更多规则类型（用户继续描述中）

## 待确认（讨论中）

- **多上游健康检查**：主动探测 vs 被动失败计数的具体策略、探测间隔与失败阈值
- **规则 action 是否迁移 Go**：模块化完成后，按实际性能基准评估
  （当前低性能要求下不迁移）

## 已确认的技术决策（2026-08-29）

- **Redis 缓存抽象**：暂缓，保持 APCu 本地；后续多实例部署时再评估
- **规则 action 实现**：低性能要求下继续在 PHP worker 内实现，但需模块化
  （Action 接口 + 注册表），便于后续评估是否用 Go 实现
- **热路径不触数据库**：网关转发 / 缓存路径不得同步查询 SQLite；
  SQLite 仅用于管理端、配置存储、后台聚合与维护任务
- **记录链路（方案 1 确认）**：热路径只追加写本地 JSON 日志文件（不触 SQLite），
  由独立 PHP CLI 进程后台导入 SQLite 并聚合统计
- **统计定位**：SQLite 统计仅服务管理目的（API / 客户端清单、趋势、后台展示），
  允许分钟级延迟，不参与任何在线判定
- **限流与自动拉黑与统计解耦**：限流判定、自动拉黑只依赖热路径内存计数（APCu），
  绝不依赖异步统计链路；自动拉黑 APCu 立即生效 + 事件异步持久化
- **日志文件轮转**：按天轮转，保留天数与 `retention.raw_days` 对齐（待细化）
- **正式安装方式（2026-08-30 确认）**：仅使用命令行
  `deploy/install-frampp.sh`（安装到 FRAMPP_HOME/apps，不在 web 根下）；
  web 安装器（install.php）因解压目录位于 web 根、静态文件可下载
  （SQLite/日志/明文令牌）等风险，默认不生成、仅限本地开发自举

## 热路径 SQLite 审计（2026-08-29）

与“热路径不触库”冲突的现状：

- `Recorder::registerApi()`：每请求 INSERT+SELECT `apis` 表
  （access deny 与 filter 两个阶段都会调用）
- `Recorder::flush()`：worker 每请求 `finally` 强制 flush，批次配置被绕过，
  实际每请求同步 INSERT `request_logs`
- `Auth::checkToken()`：命中 token 认证规则的每请求 SELECT `tokens` 表
- `Blacklist::addAuto()`：限流超限时同步 INSERT `blacklist`（偶发）
- 低风险项：规则 / 黑名单 APCu 快照失效时的冷加载、日志清理 DELETE

解决方向（待细化）：

- API 清单与统计由后台聚合从日志派生，不在请求路径维护
- 请求记录改异步：本地 JSON 日志文件 + 独立进程导入（方案 1 已确认）
- `tokens` 走 APCu 快照 + 管理端写时失效（同 rules / blacklist 机制）
- 自动拉黑、日志清理移出请求路径（后台任务）
